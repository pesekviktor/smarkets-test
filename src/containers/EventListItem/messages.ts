import {defineMessages} from 'react-intl';

export const scope = 'app.containers.EventListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '',
  },
});
